# Cargo audit overrides for known acceptable vulnerabilities

# rustls-pemfile is unmaintained but acceptable for our use case
# We monitor for security issues and the alternatives are not yet mature
# This dependency is used only for TLS certificate loading
[advisories]
RUSTSEC-2025-0134 = "accepted"